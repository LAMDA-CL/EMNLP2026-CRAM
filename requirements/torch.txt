# PyTorch wheels — CUDA 12.8 (RTX 5090 / Blackwell and other cu128 builds).
# Install FIRST, before other requirements:
#   pip install -r requirements/torch.txt
#
# Legacy A100 / CUDA 11.8 (torch 2.0.1) — see requirements/torch-cu118.txt

--extra-index-url https://download.pytorch.org/whl/cu128
torch==2.8.0
torchvision==0.23.0
torchaudio==2.8.0
