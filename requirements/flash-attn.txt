# FlashAttention for train_mem.py / llama_flash_attn_monkey_patch.py
# Prefer a prebuilt wheel matching torch 2.8 + cu128 + cp310 when available, e.g.:
#   pip install flash_attn-2.8.3+cu12torch2.8cxx11abiFALSE-cp310-cp310-linux_x86_64.whl
# Or set FLASH_ATTN_WHEEL=/path/to/whl when running scripts/setup_env.sh
#
# Source build fallback (needs matching GCC/nvcc):
flash-attn==2.8.3
